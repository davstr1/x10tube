extends layout

block content
  section.mb-12
    if error
      .bg-red-900.dark_bg-red-950.border.border-red-700.rounded-md.p-4.mb-6.text-red-200.text-sm
        p= error
        if failedUrls && failedUrls.length > 0
          ul.mt-2.list-disc.list-inside
            each fail in failedUrls
              li #{fail.url}: #{fail.error}

    form(action="/create" method="POST")
      textarea.w-full.px-3.py-2.border.border-border-strong.rounded-md.text-base.focus_outline-none.focus_border-text-muted.resize-none.font-mono.text-sm.bg-surface.text-text-primary(
        name="urls"
        rows="5"
        placeholder="https://youtube.com/watch?v=...\nhttps://example.com/article\n(one URL per line, YouTube or any web page)"
        required
      )
      button.mt-4.w-full.bg-red-600.hover_bg-red-700.text-white.px-4.py-3.rounded-md.text-sm.font-medium(type="submit")
        | Create a X10

    p.mt-4.text-sm.text-text-secondary.text-center
      | or install the #[a.text-red-600.underline(href="#extension") Chrome extension] to collect videos as you browse

  section.py-8.border-t.border-border-strong
    p.text-lg.text-text-secondary.mb-2
      | Summarize and discuss YouTube videos and web pages
      br
      | in Claude, ChatGPT, Gemini, Grok, or your favorite AI assistant.
    p.text-text-muted
      | 10 items. 10 minutes. Instead of 10 hours.

  section.py-8.border-t.border-border-strong
    h2.text-xl.font-semibold.mb-4 Why X10Tube?
    ul.space-y-2.text-text-secondary
      li No built-in chat to pay for
      li Your LLM already knows you
      li Easy to share

  section.py-8.border-t.border-border-strong
    h2.text-xl.font-semibold.mb-4 How it works
    ol.space-y-2.text-text-secondary.list-decimal.list-inside
      li Paste your URLs (YouTube videos or web pages)
      li Open the content compilation in your AI assistant
      li Ask your questions

  section.py-8.border-t.border-border-strong
    h2.text-xl.font-semibold.mb-4 FAQ

    .space-y-4
      div
        h3.font-medium Is it free?
        p.text-text-secondary Yes.

      div
        h3.font-medium Does it work with Claude/ChatGPT/...?
        p.text-text-secondary Yes, and with any AI. Use "Open in..." or copy-paste the content.

      div
        h3.font-medium How many items max?
        p.text-text-secondary
          | We suggest 10, but it's a soft limit. The real limit depends on your LLM's capacity. Claude can read ~100K tokens. We show an estimate on each x10.

      div
        h3.font-medium Is my data stored?
        p.text-text-secondary x10s are public. Log in to edit or delete.
