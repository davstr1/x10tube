extends layout

block content
  //- Breadcrumb
  nav.mb-4
    a.text-sm.text-text-muted.hover_text-text-primary.flex.items-center.gap-2(href="/collections")
      svg(viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="width:16px;height:16px;")
        polygon(points="12 2 2 7 12 12 22 7 12 2")
        polyline(points="2 17 12 22 22 17")
        polyline(points="2 12 12 17 22 12")
      | My collections

  //- Header section
  section.mb-6
    //- Title (editable if owner)
    if isOwner
      form#title-form(action=`/s/${x10.id}/title` method="POST")
        input#title-input.text-2xl.font-semibold.text-text-primary.bg-transparent.border-b-2.border-transparent.hover_border-border-strong.focus_border-red-500.focus_outline-none.w-full.py-1.transition-colors(
          type="text"
          name="title"
          value=x10.title || 'Untitled'
          placeholder="Give your collection a title..."
        )
    else
      h1.text-2xl.font-semibold.text-text-primary= x10.title || 'Untitled'

    p.text-sm.text-text-secondary.mt-2
      | #{x10.items.length} item#{x10.items.length > 1 ? 's' : ''}
      span.mx-2 ·
      | #{tokenDisplay}
      span.mx-2 ·
      | #{createdDate}

  //- Action buttons
  section.mb-10
    - const mdUrl = `${baseUrl}/s/${x10.id}.txt`

    style.
      .dropdown { position: relative; }
      .dropdown:hover { z-index: 100; }
      .dropdown-menu {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        z-index: 100;
        min-width: 12rem;
        padding-top: 0.25rem;
      }
      .dropdown-menu-inner {
        background: var(--color-surface);
        border: 1px solid var(--color-border-strong);
        border-radius: 0.5rem;
        box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1);
        padding: 0.25rem 0;
      }
      .dropdown-menu-inner button,
      .dropdown-menu-inner a {
        text-align: left !important;
      }
      .dropdown:hover .dropdown-menu { display: block; }

    .flex.flex-wrap.gap-3.items-center
      //- Open in... dropdown (hover to open)
      .inline-block.dropdown
        button.inline-flex.items-center.gap-2.bg-red-600.hover_bg-red-700.text-white.px-5.py-3.rounded-lg.text-sm.font-medium.transition-colors(
          type="button"
        )
          | Open in...
          span.ml-1 ▾
        .dropdown-menu
          .dropdown-menu-inner
            button.block.w-full.text-left.px-4.py-2.text-sm.text-text-primary.hover_bg-surface-hover(
              type="button"
              data-stya-open-in="claude"
              data-stya-md-url=mdUrl
            ) Claude
            button.block.w-full.text-left.px-4.py-2.text-sm.text-text-primary.hover_bg-surface-hover(
              type="button"
              data-stya-open-in="chatgpt"
              data-stya-md-url=mdUrl
            ) ChatGPT
            button.block.w-full.text-left.px-4.py-2.text-sm.text-text-primary.hover_bg-surface-hover(
              type="button"
              data-stya-open-in="gemini"
              data-stya-md-url=mdUrl
            ) Gemini#[span.text-text-muted *]
            button.block.w-full.text-left.px-4.py-2.text-sm.text-text-primary.hover_bg-surface-hover(
              type="button"
              data-stya-open-in="perplexity"
              data-stya-md-url=mdUrl
            ) Perplexity#[span.text-text-muted *]
            button.block.w-full.text-left.px-4.py-2.text-sm.text-text-primary.hover_bg-surface-hover(
              type="button"
              data-stya-open-in="grok"
              data-stya-md-url=mdUrl
            ) Grok
            button.block.w-full.text-left.px-4.py-2.text-sm.text-text-primary.hover_bg-surface-hover(
              type="button"
              data-stya-open-in="copilot"
              data-stya-md-url=mdUrl
            ) Copilot
            .px-4.py-2.text-xs.text-text-muted.border-t.border-border-default.mt-1 * Clipboard mode

      //- Copy dropdown
      - const x10Url = `${baseUrl}/s/${x10.id}`
      .inline-block.dropdown
        button.inline-flex.items-center.gap-2.bg-surface.hover_bg-surface-hover.text-text-primary.px-5.py-3.rounded-lg.text-sm.font-medium.border.border-border-strong.transition-colors(
          type="button"
        )
          | Copy...
          span.ml-1 ▾
        .dropdown-menu
          .dropdown-menu-inner
            button.block.w-full.text-left.px-4.py-2.text-sm.text-text-primary.hover_bg-surface-hover(
              type="button"
              onclick=`navigator.clipboard.writeText('${mdUrl}'); this.textContent='Copied!'; setTimeout(() => this.textContent='Link', 1500)`
            ) Link
            button#copy-md-btn.block.w-full.text-left.px-4.py-2.text-sm.text-text-primary.hover_bg-surface-hover(
              type="button"
            ) Copy MD
            button#download-md-btn.block.w-full.text-left.px-4.py-2.text-sm.text-text-primary.hover_bg-surface-hover.flex.items-center.gap-2(
              type="button"
            )
              svg(width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round")
                path(d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4")
                polyline(points="7 10 12 15 17 10")
                line(x1="12" y1="15" x2="12" y2="3")
              | Download MD

      //- Spacer to push delete to the right
      .flex-1
      if isOwner
        form.inline(action=`/x10/${x10.id}/delete` method="POST")
          button.w-8.h-8.flex.items-center.justify-center.text-text-muted.hover_text-red-500.hover_bg-red-500.hover_bg-opacity-10.rounded-full.transition-colors(
            type="submit"
            title="Delete this collection"
            onclick="return confirm('Delete this collection?')"
          ) ×

  //- Items list (videos + web pages)
  section.mb-10
    .space-y-4
      each item, index in x10.items
        - const isYouTube = item.source_type === 'youtube'
        - const thumbnailUrl = isYouTube ? `https://img.youtube.com/vi/${item.source_id}/mqdefault.jpg` : `https://www.google.com/s2/favicons?domain=${item.channel}&sz=128`
        - const itemId = isYouTube ? item.source_id : item.id
        - const itemMdUrl = `${baseUrl}/s/${x10.id}/v/${itemId}.txt`
        .border.border-border-strong.rounded-lg.bg-surface
          .flex.gap-4.p-4
            //- Thumbnail
            a.flex-shrink-0.block(href=item.url target="_blank" title=isYouTube ? "Watch on YouTube" : "Open page")
              if isYouTube
                img.w-44.h-24.object-cover.rounded-md(src=thumbnailUrl alt=item.title)
              else
                .w-44.h-24.rounded-md.bg-surface-alt.flex.items-center.justify-center
                  img.w-12.h-12(src=thumbnailUrl alt=item.channel)

            //- Item info
            .flex-1.min-w-0
              .flex.justify-between.items-start.gap-2
                div.min-w-0
                  h3.font-medium.text-text-primary.leading-snug.mb-1
                    span.text-text-muted.mr-1 #{index + 1}.
                    if !isYouTube
                      span.text-xs.text-text-muted.mr-1 🌐
                    = item.title
                  p.text-sm.text-text-secondary
                    = item.channel
                    if item.duration
                      span.mx-1 ·
                      = item.duration

                  //- Item actions
                  .flex.items-center.gap-3.mt-2
                    //- Open in... dropdown
                    .dropdown.text-xs
                      button.text-text-muted.hover_text-text-secondary(type="button")
                        | Open in...
                        span.ml-1 ▾
                      .dropdown-menu
                        .dropdown-menu-inner
                          button.block.w-full.text-left.px-3.py-1.text-xs.text-text-primary.hover_bg-surface-hover(
                            type="button"
                            data-stya-open-in="claude"
                            data-stya-md-url=itemMdUrl
                          ) Claude
                          button.block.w-full.text-left.px-3.py-1.text-xs.text-text-primary.hover_bg-surface-hover(
                            type="button"
                            data-stya-open-in="chatgpt"
                            data-stya-md-url=itemMdUrl
                          ) ChatGPT
                          button.block.w-full.text-left.px-3.py-1.text-xs.text-text-primary.hover_bg-surface-hover(
                            type="button"
                            data-stya-open-in="gemini"
                            data-stya-md-url=itemMdUrl
                          ) Gemini#[span.text-text-muted *]
                          button.block.w-full.text-left.px-3.py-1.text-xs.text-text-primary.hover_bg-surface-hover(
                            type="button"
                            data-stya-open-in="perplexity"
                            data-stya-md-url=itemMdUrl
                          ) Perplexity#[span.text-text-muted *]
                          button.block.w-full.text-left.px-3.py-1.text-xs.text-text-primary.hover_bg-surface-hover(
                            type="button"
                            data-stya-open-in="grok"
                            data-stya-md-url=itemMdUrl
                          ) Grok
                          button.block.w-full.text-left.px-3.py-1.text-xs.text-text-primary.hover_bg-surface-hover(
                            type="button"
                            data-stya-open-in="copilot"
                            data-stya-md-url=itemMdUrl
                          ) Copilot
                          .px-3.py-1.text-xs.text-text-muted.border-t.border-border-default.mt-1 * Clipboard mode

                    //- Copy... dropdown
                    .dropdown.text-xs
                      button.text-text-muted.hover_text-text-secondary(type="button")
                        | Copy...
                        span.ml-1 ▾
                      .dropdown-menu
                        .dropdown-menu-inner
                          button.block.w-full.px-3.py-1.text-xs.text-text-primary.hover_bg-surface-hover(
                            type="button"
                            onclick=`navigator.clipboard.writeText('${itemMdUrl}'); this.textContent='Copied!'; setTimeout(() => this.textContent='Link', 1500)`
                          ) Link
                          button.block.w-full.px-3.py-1.text-xs.text-text-primary.hover_bg-surface-hover.copy-item-md-btn(
                            type="button"
                            data-item-url=itemMdUrl
                          ) Copy MD
                          button.block.w-full.px-3.py-1.text-xs.text-text-primary.hover_bg-surface-hover.download-item-md-btn.flex.items-center.gap-1(
                            type="button"
                            data-item-url=itemMdUrl
                          )
                            svg(width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round")
                              path(d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4")
                              polyline(points="7 10 12 15 17 10")
                              line(x1="12" y1="15" x2="12" y2="3")
                            | Download MD

                if isOwner
                  form(action=`/s/${x10.id}/remove/${item.id}` method="POST")
                    button.w-8.h-8.flex.items-center.justify-center.text-text-muted.hover_text-red-500.hover_bg-red-500.hover_bg-opacity-10.rounded-full.transition-colors(
                      type="submit"
                      title="Remove item"
                    ) ×

          //- Content accordion
          details.border-t.border-border-default.group
            summary.px-4.py-3.text-sm.text-text-secondary.cursor-pointer.hover_bg-surface-hover.select-none.flex.items-center.gap-2
              span.text-text-muted.transition-transform.group-open_rotate-90 ▶
              | #{isYouTube ? 'Show transcript' : 'Show content'}
            .px-4.py-4.bg-surface-alt.text-sm.text-text-secondary.leading-relaxed.max-h-72.overflow-y-auto
              p.whitespace-pre-wrap= item.transcript

  //- Copy to account button (for non-owners who are logged in)
  if currentUser && !isOwner
    section.mb-10
      form(action=`/api/x10/${x10.id}/fork` method="POST")
        button.bg-surface.hover_bg-surface-hover.text-text-primary.px-5.py-3.rounded-lg.text-sm.font-medium.border.border-border-strong.transition-colors(type="submit")
          | Copy to my account

  //- Settings section (bottom)
  if isOwner
    section.border-t.border-border-strong.pt-8
      h2.text-lg.font-semibold.text-text-primary.mb-6 Collection settings

      //- Prompt for this x10
      .bg-surface.border.border-border-strong.rounded-lg.p-4
        .flex.justify-between.items-center.mb-2
          p.text-sm.text-text-secondary Prompt for this collection
          button#save-preprompt.text-xs.text-red-600.hover_text-red-700.hidden(type="button") Save
        textarea#x10-preprompt.w-full.px-3.py-2.border.border-border-strong.rounded-md.text-sm.focus_outline-none.focus_border-text-muted.resize-none.bg-surface.text-text-primary(
          rows="2"
          placeholder=defaultPrePrompt
        )= x10.pre_prompt || effectivePrePrompt
        p.text-xs.text-text-muted.mt-2 Instructions sent to the LLM after fetching the content

  script.
    (function() {
      // Title auto-save functionality
      const titleForm = document.getElementById('title-form');
      const titleInput = document.getElementById('title-input');
      if (titleForm && titleInput) {
        const originalTitle = titleInput.value;

        titleInput.addEventListener('blur', function() {
          if (titleInput.value !== originalTitle && titleInput.value.trim() !== '') {
            titleForm.submit();
          }
        });

        titleInput.addEventListener('keydown', function(e) {
          if (e.key === 'Enter') {
            e.preventDefault();
            if (titleInput.value !== originalTitle && titleInput.value.trim() !== '') {
              titleForm.submit();
            } else {
              titleInput.blur();
            }
          }
        });
      }

      // Pre-prompt save functionality
      const prepromptTextarea = document.getElementById('x10-preprompt');
      const saveBtn = document.getElementById('save-preprompt');

      if (prepromptTextarea && saveBtn) {
        const originalValue = prepromptTextarea.value;

        prepromptTextarea.addEventListener('input', function() {
          saveBtn.classList.toggle('hidden', prepromptTextarea.value === originalValue);
        });

        saveBtn.addEventListener('click', async function() {
          const form = document.createElement('form');
          form.method = 'POST';
          form.action = '/s/#{x10.id}/preprompt';
          const input = document.createElement('input');
          input.type = 'hidden';
          input.name = 'prePrompt';
          input.value = prepromptTextarea.value;
          form.appendChild(input);
          document.body.appendChild(form);
          form.submit();
        });
      }

      // Slugify for filenames
      function slugify(text) {
        return text
          .toLowerCase()
          .normalize('NFD')
          .replace(/[\u0300-\u036f]/g, '')
          .replace(/[^a-z0-9]+/g, '-')
          .replace(/^-+|-+$/g, '')
          .slice(0, 50);
      }

      // Download function
      function downloadAsMarkdownFile(content, title) {
        const slug = title ? slugify(title) : 'collection';
        const filename = 'stya-' + slug + '.md';
        const blob = new Blob([content], { type: 'text/markdown;charset=utf-8' });
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        a.download = filename;
        a.click();
        URL.revokeObjectURL(url);
      }

      // Copy MD functionality (full collection)
      const copyMdBtn = document.getElementById('copy-md-btn');
      if (copyMdBtn) {
        copyMdBtn.addEventListener('click', async function() {
          try {
            const response = await fetch('/s/#{x10.id}.txt');
            const md = await response.text();
            await navigator.clipboard.writeText(md);
            copyMdBtn.textContent = 'Copied!';
            setTimeout(() => copyMdBtn.textContent = 'Copy MD', 1500);
          } catch (e) {
            copyMdBtn.textContent = 'Error';
            setTimeout(() => copyMdBtn.textContent = 'Copy MD', 1500);
          }
        });
      }

      // Download MD functionality (full collection)
      const downloadMdBtn = document.getElementById('download-md-btn');
      if (downloadMdBtn) {
        downloadMdBtn.addEventListener('click', async function() {
          const originalHtml = downloadMdBtn.innerHTML;
          try {
            const response = await fetch('/s/#{x10.id}.txt');
            const md = await response.text();
            downloadAsMarkdownFile(md, '#{x10.title}');
            downloadMdBtn.innerHTML = 'Downloaded!';
            setTimeout(() => downloadMdBtn.innerHTML = originalHtml, 1500);
          } catch (e) {
            downloadMdBtn.innerHTML = 'Error';
            setTimeout(() => downloadMdBtn.innerHTML = originalHtml, 1500);
          }
        });
      }

      // Copy MD functionality (individual items)
      document.querySelectorAll('.copy-item-md-btn').forEach(function(btn) {
        btn.addEventListener('click', async function() {
          const itemUrl = btn.dataset.itemUrl;
          try {
            const response = await fetch(itemUrl);
            const md = await response.text();
            await navigator.clipboard.writeText(md);
            btn.textContent = 'Copied!';
            setTimeout(() => btn.textContent = 'Copy MD', 1500);
          } catch (e) {
            btn.textContent = 'Error';
            setTimeout(() => btn.textContent = 'Copy MD', 1500);
          }
        });
      });

      // Download MD functionality (individual items)
      document.querySelectorAll('.download-item-md-btn').forEach(function(btn) {
        btn.addEventListener('click', async function() {
          const itemUrl = btn.dataset.itemUrl;
          const originalHtml = btn.innerHTML;
          try {
            const response = await fetch(itemUrl);
            const md = await response.text();
            downloadAsMarkdownFile(md);
            btn.innerHTML = 'Downloaded!';
            setTimeout(() => btn.innerHTML = originalHtml, 1500);
          } catch (e) {
            btn.innerHTML = 'Error';
            setTimeout(() => btn.innerHTML = originalHtml, 1500);
          }
        });
      });
    })();
