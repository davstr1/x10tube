extends layout

block content
  section.max-w-sm.mx-auto
    h1.text-2xl.font-semibold.text-text-primary.mb-6.text-center Log in

    form(action="/auth/login" method="POST")
      label.block.text-sm.text-text-secondary.mb-2(for="email") Email
      input.w-full.px-3.py-2.border.border-border-strong.rounded-md.text-base.focus_outline-none.focus_border-text-muted.bg-surface.text-text-primary(
        type="email"
        id="email"
        name="email"
        placeholder="you@example.com"
        required
      )
      button.mt-4.w-full.bg-red-600.hover_bg-red-700.text-white.px-4.py-3.rounded-md.text-sm.font-medium(type="submit")
        | Send magic link

    p.mt-6.text-sm.text-text-secondary.text-center
      | We'll send you a link to log in. No password needed.
