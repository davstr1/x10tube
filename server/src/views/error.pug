extends layout

block content
  .text-center.py-16
    h1.text-4xl.font-semibold.text-gray-900.mb-4= title || 'Error'
    p.text-gray-600.mb-8= message || 'Something went wrong'
    a.text-blue-600.underline(href="/") Back to home
