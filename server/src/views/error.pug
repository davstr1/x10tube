extends layout

block content
  .text-center.py-16
    h1.text-4xl.font-semibold.text-text-primary.mb-4= title || 'Error'
    p.text-text-secondary.mb-8= message || 'Something went wrong'
    a.text-red-600.underline(href="/") Back to home
